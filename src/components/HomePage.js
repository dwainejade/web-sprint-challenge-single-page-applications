import React from 'react';
import styled from 'styled-components';
import { Link } from 'react-router-dom';

const HomePage = props => {
    return (
        <header>
            <h1>If You Build It, They Will Consume</h1>
        </header>
    )
}

export default HomePage;